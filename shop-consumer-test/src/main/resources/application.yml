server:
  port: 9001

spring:
  application:
    name: consumer

logging:
  level:
    com.zmm.gethub.client.ConsumerClient : debug

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka.didispace.com/eureka/
